import PropTypes from 'prop-types';

export const justifyContentProp = { justifyContent: PropTypes.oneOf(['left', 'right', 'center']) };

export const textTransformProp = { textTransform: PropTypes.oneOf(['uppercase', 'lowercase', 'capitalize', 'none']) };